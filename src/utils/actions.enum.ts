export enum AdsAction {
  CREATE = 'CREATE',
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}

export enum FeaturePropertyAction {
  CREATE = 'CREATE',
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}

export enum BrokerRequestAction {
  CREATE = 'CREATE',
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}

export enum PropertyReporttAction {
  CREATE = 'CREATE',
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}
export enum ListingPackageAction {
  CREATE = 'CREATE',
  APPROVE = 'APPROVE',
  REJECT = 'REJECT',
}

export enum ReportPropertyAction {
  NEW = 'NEW',
  VIEWED = 'VIEWED',
  PENDING = 'PENDING',
  CORRECTED = 'CORRECTED',
  APPROVED = 'APPROVED',
  ALL = 'ALL',
}
